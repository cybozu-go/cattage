apiVersion: v1
kind: Pod
metadata:
  name: testhttpd
  namespace: sub-1
spec:
  containers:
  - image: quay.io/cybozu/testhttpd:0.1.6
    imagePullPolicy: IfNotPresent
    name: testhttpd
    ports:
    - containerPort: 8000
      protocol: TCP
